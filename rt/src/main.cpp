#include <camera.hpp>
#include <hittable_list.hpp>
#include <rt.hpp>

#include <memory>

// We draw the image from top left corner across and then down

int main() {

  // many_balls_scene();

  checkered_spheres();

  return 0;
}
